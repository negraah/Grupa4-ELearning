@model IEnumerable<E_Learning.Models.Odgovor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JeLiTacno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pitanje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kviz)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JeLiTacno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pitanje.PitanjeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kviz.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
